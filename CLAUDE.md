NORTH STAR: NORTH_STAR.md is the product plan and decides priorities. This repo (the web app) owns 'Priority 1: Fix this week'. Before starting any task, read it and state which item the task moves forward. If a request conflicts with it or isn't in it, say so before building. When you finish an item in your section, tick it with the date and commit hash. Don't tick items in 'Priority 2', which is the iOS app's section in a separate repo. Any change to the shared Supabase database (new tables, columns, triggers, data fixes) must be flagged to Jonny before applying, because the iOS app uses the same database. This repo deploys straight to production on push, so test Stripe and webhook changes in Stripe test mode before pushing.
